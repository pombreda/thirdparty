language: cpp

sudo: required

os: 
  - osx
  - linux 

install:
    - sudo pip install --upgrade pip wheel

script:
    - python -V
    - gcc -v
    - ls -al
    - mdkdir wheels
    - cd pyahocorasick
    - pip wheel pyahocorasick intbitset bitarray --wheel-dir=wheels
    - ls -al
    - ls -al wheels